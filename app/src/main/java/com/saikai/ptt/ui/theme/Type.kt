package com.saikai.ptt.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography. The product is a walkie-talkie, not a
// branded consumer app: legibility on low-end screens matters more than a
// custom type scale. See docs/04_UI_UX.md section 4.
val SaikaiTypography = Typography()
