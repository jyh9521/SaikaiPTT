# Changelog

本文件记录对用户可见的变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

`versionName` 与 `versionCode` 定义在 `app/build.gradle.kts`。

---

## [未发布]

### 待办

发布前必须完成，见 `docs/11_ReleaseSignOff.md`：

- 两台设备的联调测试（`docs/09_TestReport.md §3.3`）
- 四台设备的心跳流量与强插竞态验证（`§3.4`，自 Task29 起阻塞）
- 低端设备上的性能测量（`docs/10_PerformanceMeasurement.md`）
- 缅甸语与孟加拉语译文的母语者审阅，以及低端机上的字体渲染确认

---

## [0.1.0] — 未发布

第一个完整实现。功能全部就位，真机验证尚未完成，因此**不是** Release Candidate。

### 新增

**通信**

- 同一 WiFi 局域网内的一对一 Push-To-Talk 语音，按住讲话、松开结束（半双工）
- UDP 广播自动发现设备，不需要手动输入 IP、主机名或注册设备
- 心跳与在线状态，独立于设备发现
- 忙线拒绝；**接收方**可选开启「允许被打断」，呼叫方无法单方面强插
- WiFi 断开与 IP 变化后自动恢复：标记设备离线、停止传输、重连后重建设备列表
- 72 字节定长包头的二进制协议，设备与会话标识为 16 字节二进制 UUID

**后台**

- 前台服务常驻，**后台、熄屏、锁屏均可接收**
- 讲话需要界面可见（Android 14 起禁止从后台提升为 microphone 类型前台服务）
- 开机自启（仅接收）
- 可选悬浮窗指示，显示通话状态与对方名字

**身份与名字**

- 首次启动生成 UUID v4 作为设备标识，与 IP 地址解耦
- 本机可保存多个名字，可新建、改名、删除、切换
- 名字随通话发送，对端无需在本地存过

**录音与历史**

- 每次成功完成的通话在双方各生成一条记录
- 录音为 Ogg/Opus，与通话共用同一份编码结果，不重复编码
- 回放：播放 / 暂停 / 继续 / 停止 + 进度
- 按对方名字与字幕内容搜索
- 未读标记、收藏、单条删除、批量删除
- 保留期 1 / 3 / 7 / 30 天 / 永久，默认 7 天；收藏永不被自动清理
- 自动清理会同步删除音频文件，并回收没有记录指向的孤立文件
- 正在录制与正在播放的文件不会被清理删除

**界面**

- Material Design 3，五种语言：日本語 / 简体中文 / English / မြန်မာ / বাংলা
- 状态不依赖颜色单独表达

### 不包含

- **离线日语字幕**。引擎复核未通过，见 `docs/ADR/ADR-011-ASR-Engine-Review.md`。
  数据模型保留了 `transcript` 与五态的 `transcriptStatus`，将来补上引擎不需要改数据库。
- 群组通话、文字消息、文件传输。不在 v1 范围内。
- AP Isolation 环境下的手动 IP 补救手段。

### 隐私

- **不访问互联网。** 网络流量只有局域网内的对讲协议包。
- 录音与历史记录只写应用私有目录，不上传、不云同步。
- 无遥测、无分析 SDK。
- 系统备份已关闭（`allowBackup="false"`）。

### 已知限制

见 `README.md` 的「已知限制」一节。

---

[未发布]: https://github.com/jyh9521/SaikaiPTT/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/jyh9521/SaikaiPTT/releases/tag/v0.1.0
